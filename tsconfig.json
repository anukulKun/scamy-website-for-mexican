{
  "compilerOptions": {
    // Include necessary TypeScript libraries for DOM and ESNext features.
    "lib": ["dom", "dom.iterable", "esnext"],

    // Allow JavaScript files in the project.
    "allowJs": true,

    // Skip checking declaration files in the node_modules folder.
    "skipLibCheck": true,

    // Disable emitting output files (handled internally by Next.js).
    "noEmit": true,

    // Enable interoperability between CommonJS and ES Modules.
    "esModuleInterop": true,

    // Set module to 'esnext' for compatibility with modern ES module features.
    "module": "esnext",

    // Use 'bundler' for module resolution (for Next.js module bundling).
    "moduleResolution": "bundler",

    // Resolve JSON modules to allow importing .json files.
    "resolveJsonModule": true,

    // Treat every file as a separate module (important for Next.js).
    "isolatedModules": true,

    // Keep JSX syntax intact (Next.js will handle JSX transformation).
    "jsx": "preserve",

    // Enable incremental builds for faster builds during development.
    "incremental": true,

    // Next.js-specific TypeScript plugin.
    "plugins": [
      {
        "name": "next"
      }
    ],

    // Setup path aliases, allowing `@/` to refer to the root directory.
    "paths": {
      "@/*": ["./*"]
    },

    // Disable strict type checks (set to true for strict mode).
    "strict": false
  },
  
  // Include Next.js environment types and all TypeScript/TSX files.
  "include": [
    "next-env.d.ts",
    "**/*.ts",
    "**/*.tsx",
    ".next/types/**/*.ts"
  ],

  // Exclude node_modules from type checking.
  "exclude": [
    "node_modules"
  ]
}
